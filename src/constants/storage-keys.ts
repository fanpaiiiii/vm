/** localStorage key constants — single source of truth */
export const STORAGE_KEYS = {
  MANUAL_RATES: 'foreign_trade_manual_rates',
  SHIPPING_CHANNELS: 'foreign_trade_shipping_channels',
} as const
